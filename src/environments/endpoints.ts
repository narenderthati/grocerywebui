<<<<<<< HEAD
import {environment} from './environment'

export const endpoints = {
    get_app_menu : `${environment.environmentIP}/appmenu`

import { environment } from './environment';

export const endpoints= {
    gwa_appmenu : `${environment.appendpointIP}/appmenu`,
    search_product : `${environment.appendpointIP}/searchproduct`
}
